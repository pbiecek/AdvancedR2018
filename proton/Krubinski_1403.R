library(dplyr)
library("BetaBit")
proton()

slap

e <- employees
e[e$name=='John',]

proton(action='login', login='johnins')
p <- top1000passwords

for (c in p){
  proton(action = 'login',login='johnins',password=c)
}
l <- logs
l
l[l$login == 'slap',] %>% count(host)
 
proton(action='server',host='194.29.178.16')

b <- bash_history
unique(gsub(' .*','',b))
proton(action='login',login='slap',password="DHbb7QXppuHnaXGN")
